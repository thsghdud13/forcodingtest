#include <bits/stdc++.h>
using namespace std;

int n,d,k,c;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin>>n>>d>>k>>c;
    vector <int> sushi;
    for(int i = 0; i < n; i++){
        int s;
        cin>>s;
        sushi.push_back(s);
    }

    int mx = 0;
    int en = 0;

    for(int st = 0; st < n; st++){
        while(en < n){
            
        }
        if(st == n-1) st = 0;
    }

}